# sets
age = [22, 19, 24, 25, 26, 24, 25, 24]

# 1
age_set = set(age)

# 2
print('String is a data type, list uses the [], kind of like an array, tuple uses () and they both store elements in '
      'single and multiple rows and columns. They are al non-homogenous'
      'sets uses {} and doesn\'t accept duplicates but stores only in a single row')

# 3
sentence = 'I am a teacher and I love to inspire and teach people'
sentence = sentence.split()
sentence_set = set(sentence)
print(sentence_set)

